# ============================================================
# Playday — Electron 项目标准忽略规则
# ============================================================

# ---- Node / 依赖 ----
node_modules/

# ---- 构建产物（前端 + 主进程 + 打包输出）----
dist/
dist-electron/
out/
build/
*.tsbuildinfo

# ---- Vite / 打包缓存 ----
*.local
.eslintcache
.vite/

# ---- 日志 ----
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---- 编辑器 / 系统 ----
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

# ---- 环境变量 / 密钥（防止泄露）----
.env
.env.*
!.env.example
*.pem
*.key
*.p12

# ---- 测试覆盖率 / 临时文件 ----
coverage/
.tmp/
temp/

# ---- 本地开发配置（不入仓库）----
# .codebuddy/ 是本机目录（skills 体积大、且可从 marketplace 重装），默认不入库。
# 但 rules/ 必须入库：它是"项目记忆在仓库里"的指路牌 —— 重装系统后新会话靠它找到
# docs/PROJECT-MEMORY.md。注意 gitignore 的语义：想放行一个子目录，必须让 git 能进到
# 父目录，所以这里写 `.codebuddy/*`（而不是 `.codebuddy/`，后者会让 git 根本不进这个目录，
# 后面的 `!` 规则会失效）。
.codebuddy/*
!.codebuddy/rules/
.workbuddy/
.prototools
*.log
dev-client-err.log

# ============================================================
# tools/GameSaveHelper（搬进来的独立项目）
#   它原本是一个独立 git 仓库（origin: github.com/NemecSoft/GameSaveHelper）。
#   搬进来时把它的 .git 改名成 .git_disabled 保留着 —— 不这么做的话，git 会把
#   整个目录当成"嵌套仓库"，一个文件都纳不进来。历史在 GitHub 上，确认无误后
#   可以直接删掉 .git_disabled（约 7MB）。
#   它自己的 .gitignore（tools/GameSaveHelper/.gitignore）负责排除编译产物
#   （*.obj / *.res / *.exe / build/ / out/ / release/ / logs/ / _test/ 等）。
# ============================================================
tools/GameSaveHelper/.git_disabled/

# ============================================================
# 打包产物目录：全部可重新生成（package.bat / prepare-release），整目录不入库。
#   release/      正式包（X 盘 config.json + 随包 data/）—— 直接拷去生产机的那份
#   release_test/ 预发布包（D 盘 config.json，不随包带数据）
#   两者只差 config.json 里的盘符，所以**必须分开放**：混在一个目录里就分不清
#   哪个包能上生产机。
#   .pack-tmp/    electron-builder 的中转目录（打包成功后自动删除，只在失败时残留）
# 开发/测试态的数据在 dev-data/ —— 见下。
# ============================================================
release/
release_test/
.pack-tmp/

# ============================================================
# dev-data/：开发/测试态的数据根（config.json 的 libraryDir / sourceLibraryDir /
# announcementsDir 指向它）。**以前它是 release/data** —— 那让"打包产物"和"数据"
# 挤在同一条路径上：清一次打包目录就等于清数据（2026-09-14 拆开）。
#   Admin/library.db      权威库：数据唯一来源，只读（db.ts 启动时复制给运行时副本）
#   library/library.db    运行时副本：入库（体积小、跟代码演进；克隆下来就能跑）
#   announcements/        公告 announcement.html，入库
#   litedb-games.json     旧 LiteDB 导出（gen-game-content.mjs 的输入）
#   _archive/             从旧 release/data 抢救出来的留档（一次性，可删）
# 封面图不在这里 —— 那是 <coverImagesDir> 的事（见 config.json）。
# ⚠️ 权威库必须入库：运行时副本损坏 + 权威库缺失时 db.ts 会**直接报错**
#    （"无法自动恢复"），克隆出来的仓库根本起不来。
# ============================================================
dev-data/*
!dev-data/Admin/
dev-data/Admin/*
!dev-data/Admin/library.db
!dev-data/library/
dev-data/library/*
!dev-data/library/library.db
!dev-data/announcements/

# ============================================================
# data/（仓库根）：**故意入库**，别加忽略规则
# 这里放人工维护、丢失不可恢复的内容数据：
#   library/  整库 JSON（一表一文件：games.json / users.json / …）—— 库的可编辑镜像，
#             由 npm run db:export 生成、npm run db:import 回写（见 docs/design/library-json.md）
#   batches/  手写简介的批次小文件（merge-authored-intros.mjs 合并进 games.json）
# 它原本在 release/ 那个目录树下（那里按上面规则只放行 library、announcements、
# config.json），等于没进版本管理 —— 所以固定在仓库根。
# ============================================================

# ============================================================
# 真引擎探针的**产物**：_probe-detail-theme/site 是跑的时候从 D:/Addons 拷来的
# 页面副本 + 封面图（约 1MB），result.txt 是量出来的原始数字。
# 探针脚本本身（run.mts / probe.js）要入库；产物重新跑一次就有，别占版本。
# ============================================================
_probe-detail-theme/site/
_probe-detail-theme/result.txt

# 图标改色试验台（2026-09-16 重生成快捷方式图标时用的）：
# 拆帧脚本 + 两版候选图 + Windows 加载器渲染出来的验证图。
# 正式工具已经落在 tools/yungamestart/assets/make-icons.mjs，这里是过程证据，可随时删。
_probe-icons/
