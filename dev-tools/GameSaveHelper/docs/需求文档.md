根据D:\AI\nsis\nsis，
动态生成一个游戏存档助手，
按照参数指定的路径和通配符，备份文件
比如
GameSaveHelper.exe 大富翁11 "D:\games\Z\Richman 11\2074800\*.*" "D:\games\Z\Richman 11\settings\*.*"
就可以备份存档为exe，
默认放在系统桌面C:\Users\Administrator\Desktop

形成这样的一个nsi模板，然后自动填充，
可以自动填充1个参数的，也可以2个参数的，也可以3个参数的，
也就是，做成通用的，用nsis系统自动编译的。

形成的安装包exe，
不要那种复杂的安装，即不要产品页面，不要欢迎等等，
只要一步到位，
按钮名字叫还原备份，
显示要还原的路径（原路径）给用户看到，也可以修改
如果要覆盖，要用户确认
exe的名字是：第一个参数（游戏名）+时间（精确到秒）.exe

GameSaveHelper做成exe的，要漂亮，小，启动快，c++ 现代ui，成熟的
有异常，要报出异常（比如没有文件或者文件在锁定），界面整体变成明亮的红色
正常，要显示出形成存档的进度，界面整体变成明亮的绿色
并有成功、使用提示，
文件形成在哪里，怎么使用，
这个要用鲜艳的文字提示出来


启动一定是有参数才能启动，没有参数就显示空白窗口。
启动严格检查参数形式，不要推断，第一个是游戏名，后面的参数都要是""包裹的文件路径
为了成熟方案
有两种方式，
1、如果参数只有游戏名
比如GameSaveHelper.exe 大富翁11
则到当前目录（或指定目录）的config.json里找到游戏对应的存档路径配置，作为打包游戏存档的依据
2、第一个是游戏名，后面的参数都要是""包裹的文件路径
就用后面的文件路径参数，作为打包游戏存档的依据

界面文字参考 GameSaveHelper_old.bat


"savePaths": []
测试
1、如果是空，提示存档配置为空，请联系管理员——双点校园
2、如果是1条，正常形成存档备份——影子诡局：被诅咒的海盗
3、如果是2条，正常形成存档备份——大富翁11
4、如果成3条，正常形成存档备份——医院666-网吧联机版，有3条，但其中一个是空目录


