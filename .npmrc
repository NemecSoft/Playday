; Electron 二进制改走国内镜像（默认从 GitHub 下载，国内慢且大文件常下断）。
; 这一项管的是 npm install 时 electron 包自身的 postinstall 下载（@electron/get 读 electron_mirror）。
; 经 npm 调起的 electron-builder 也会读到它（npm 会导出 npm_config_electron_mirror，
; app-builder.exe 认这个变量名）。
;
; 打包脚本 package.bat 里另设了 ELECTRON_MIRROR / ELECTRON_BUILDER_BINARIES_MIRROR：
; 它直接调 node_modules\.bin\electron-builder.cmd，不经过 npm，所以读不到这里的 npm 配置。
;
; 只加这一个键，**不动 registry** —— 依赖解析来源不变，package-lock.json 不受影响。
; 想临时回官方源：设同名环境变量覆盖即可（环境变量优先级高于 .npmrc），
; 例如 set "ELECTRON_MIRROR=" 。
electron_mirror=https://npmmirror.com/mirrors/electron/
